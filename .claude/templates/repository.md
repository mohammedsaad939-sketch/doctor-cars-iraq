# Repository (New Service/Package) Template

Use this only if a change genuinely needs a new repository/package (e.g. splitting out a backend
service) — this project is currently a single frontend repo with no server code, so this should be
rare and deliberate.

## Name


## Why this can't live in the existing repo


## Ownership / deployment
-

## Relationship to doctor-cars-iraq
(Does it consume/produce data this frontend reads? Document the contract.)
