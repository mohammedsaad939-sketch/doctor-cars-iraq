# Page (Screen) Template

Doctor Cars Iraq has no router — a "page" is a screen registered in `App.jsx`'s `renderScreen()`
switch. See the `automotive-platform` skill before filling this in.

## Screen id (camelCase)


## Needs a back header? (added to `screensWithBack`?)


## Icon + title (added to `SCREEN_ICONS` / `SCREEN_TITLES`)


## Props received from App.jsx


## Navigation entry points (who calls `navigate("<id>", meta)`)


## Data fetched on mount


## RTL verified?
