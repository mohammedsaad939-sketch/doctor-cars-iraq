# Component Template

## Component name


## Reuses existing primitives?
(`Btn`/`Card`/`Badge`/`Input`/`Modal`/`Tabs`/`Section` from `utils/components.jsx` — check before
writing a new one-off.)

## Props
| Prop | Type | Required | Description |
|---|---|---|---|
| | | | |

## Styling
- Uses `T` tokens from `utils/theme.js` only (no new hardcoded hex values).

## Behavior
-

## Test plan
- Renders without throwing.
- Key interaction(s) fire the expected callback(s).
