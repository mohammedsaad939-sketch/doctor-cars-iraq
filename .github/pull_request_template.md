## Summary
-

## Related skills / agents
(e.g. `automotive-security`, `vehicle-data` — see `.claude/skills/`)

## Changes
-

## Test plan
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Manually verified in RTL Arabic (default); English toggle checked if strings were translated
- [ ] Reviewed against `.github/copilot-instructions.md`'s six patterns (if this touches a Supabase
      mutation, RLS policy, or admin-gated code)

## Screenshots (if UI-facing)
